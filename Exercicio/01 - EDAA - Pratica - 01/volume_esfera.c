//c. O volume de uma esfera: V = 3⁄4 * π * R3

#include <stdio.h> // printf(), scanf()
#include <math.h> // ()

int main() {
    float n, multi,resul;
    // Constante 
    multi = 1++

    printf("Informe um número : ");
    scanf("%f", &n);

    // Calculo 
   resul = (mult * n);

    printf("Resultado: %.2f\n", V);

    return 0;
}
